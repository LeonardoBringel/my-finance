# PostgreSQL connection
DB_HOST=localhost
DB_PORT=5432
DB_NAME=my_finance
DB_USER=postgres
DB_PASSWORD=your_password

# Fernet encryption key — generate with: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
FERNET_KEY=your_fernet_key_here

# JWT signing secret (separate from FERNET_KEY) — generate with: python -c "import secrets; print(secrets.token_urlsafe(48))"
JWT_SECRET=your_jwt_secret_here

# fail2ban
ENABLE_FAIL2BAN_LOGGING=false
FAIL2BAN_LOG_PATH=/var/log/my-finance/auth.log
